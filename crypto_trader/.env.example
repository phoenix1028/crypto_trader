# AI加密货币交易Agent环境配置

# === 必需配置 ===
# OpenAI API密钥（用于LLM推理）
OPENAI_API_KEY=your_openai_api_key_here

# === 可选配置 ===
# OpenAI基础URL（如果使用自定义部署的OpenAI兼容服务）
OPENAI_BASE_URL=https://api.deepseek.com/v1

# 使用的模型名称
OPENAI_MODEL=deepseek-chat

# Agent标识符（用于状态持久化）
AGENT_SIGNATURE=crypto_trader_001

# === 交易策略参数 ===
# 最大同时持仓数量
MAX_POSITIONS=2

# 杠杆倍数
LEVERAGE=20

# 止损百分比（0.015 = 1.5%）
STOP_LOSS_PCT=0.015

# 决策检查间隔（秒，3分钟=180秒）
CHECK_INTERVAL=180

# 三层置信度系统 (Alpha Arena v2.0)
# 高置信度阈值：只有置信度>此值才执行2%风险交易 (默认: 0.75)
# 中置信度阈值：置信度在此范围内执行1%风险交易 (默认: 0.6)
# 这些值在 config.py 中定义，可通过环境变量覆盖

# === 市场数据配置 ===
# 数据源选择: coingecko 或 binance
DATA_SOURCE=coingecko

# CoinGecko API (无需配置)
# 免费额度: 50次/分钟
# 优势: 简单稳定，无需API密钥

# Binance API (真实交易时需要)
# 申请地址: https://www.binance.com/en/my/settings/api-management
# 免费额度: 1200次/分钟
# 优势: 实时数据，专业交易对
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_SECRET_KEY=your_binance_secret_key_here

# === LangSmith配置（可选）===
# 用于AI决策追踪和监控
LANGCHAIN_TRACING_V2=true
LANGCHAIN_PROJECT=crypto_trader_v2
LANGCHAIN_API_KEY=your_langchain_api_key_here
